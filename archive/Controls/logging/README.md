# Data Logging with the Crazyflie

1. Add "config.txt" on the SD card
2. Before or during the flight, set the "usd.logging" parameter to 1
3. After or during the flight, set the "usd.logging" parameter to 0
4. Copy the resulting binary file
5. An example to decode the data is given in plot_events.py