# Meeting — 2026-09-12

**Since last meeting:** first meeting using this format — summarizing the whole project to date
**Attendees:**

---

## 1. Summary

*Comparison of Control Strategies for Interaction-Force Aware Multirotor Teams.* Software
preparation is finished: geometric SE(3) and INDI controllers are implemented, tuned, and
commissioned on Crazyflie 2.1 hardware (standard, thrust-upgraded, and brushless); the
16-scenario formation library, the simulator, and the residual-learning pipeline are all built
and validated in simulation. The writing track (Chapters 1–5) is drafted and frozen. Both
tracks now block on the same thing: real multi-drone flight data. The lab has just cleared a
major milestone — the first-ever 2-drone flight — but a controller-validation question (a
residual oscillation under INDI trajectory tracking) is still open and the gains are not yet
frozen, which is what stands between here and starting the actual data-collection campaign
(C.1).

## 2. What I did since last time

**Controller & platform commissioning** (this spans many weeks, see `docs/07` History for the
full log):
- Geometric SE(3) and all 4 INDI modes (geometric / position-INDI / attitude-INDI / full INDI)
  implemented as one Rust out-of-tree controller, running identically on hardware and in the
  Crazyswarm2 simulator (same source, via SIL bindings).
- Three physical platforms supported from one switchable config: standard CF2.1,
  thrust-upgraded CF2.1, brushless CF21BL (currently the primary flown platform).
- Residual force measurement (`indi.a_res_* = a_meas − a_model`, the thesis's core signal)
  implemented and logged in every controller mode, not just INDI.
- Two real flight-code bugs found and fixed: position INDI was reinforcing disturbances
  instead of cancelling them (exactly 2.00× on a known test force), and `a_res` was dead under
  the geometric controller, which would have silently blocked half the planned dataset.
- Learned-residual infrastructure built end to end: an onboard deep-sets network (987
  weights), a training pipeline, and a full collect→train→upload→enable→measure dry run —
  proven in simulation, not yet exercised on real data.
- 16-scenario formation library (single/multi-drone downwash geometries from the literature),
  fully validated in simulation on both controllers (34/34 cases).

**Controller validation, 2026-09-09 → 09-12** (see `docs/FLIGHT_CARD_VALIDATION.md` and
`docs/lab_sessions/`):
- 2026-09-09: six hover flights crashed under both controllers; three real root causes found
  and fixed (a residual sign flip that turned out to destabilize hardware and was parked
  behind a runtime flag; a sample-rate bug in the attitude filters; geometric silently flying
  with the wrong position gains).
- 2026-09-11: re-flew clean (stage 1). Stage 2 (isolating a residual oscillation under
  trajectory tracking) made real progress — bisecting the attitude gains brought peak
  oscillation from 88° down to 7° — but never reached a fully clean pass. A notch-filter
  attempt crashed outright.
- 2026-09-12: ran the card's own H0 diagnostic (attitude-INDI-only vs. position-INDI-only) —
  inconclusive; both sub-loops were individually worse than the combined system, so the two
  loops are too coupled to isolate this way. The notch-filter crash was root-caused offline
  the same day (a missed filter warm-up at the controller handover) and fixed in firmware —
  not yet flown.

**First-ever 2-drone flight, 2026-09-12** (see `docs/lab_sessions/2026-09-12.md`):
- Found and fixed six real, durable bugs in the multi-drone flight script along the way
  (missing gain selection, a mid-air controller-identity-switch risk, no hover support,
  arming gated behind a flag, a hardcoded log path that crashed flights on a different lab
  machine, a landing collision for stacked formations).
- First fully clean 2-drone flight (stock controller) showed a real physical downwash
  signature: the lower drone's roll std was 17.9° vs. the upper drone's 0.75°.
- A follow-up attempt on our own controller crashed both drones. Root-caused carefully (not
  assumed) to two compounding issues: a shared firmware config that had silently drifted
  toward one platform's tuning over two months, and a deeper bug where a drone's per-robot
  override was being silently overwritten every flight — now fixed generally.

## 3. Where I am now

- **Done:** all software preparation; formation library; simulation; residual measurement
  path; residual-learning infrastructure (sim-proven); first 2-drone flight (infrastructure
  validated, six real bugs fixed).
- **Open, blocking:** the residual trajectory-tracking oscillation under INDI has not reached
  a clean pass (stage 2 of the controller-validation card). Gains are not frozen. The notch
  crash fix has not been flight-tested.
- **Next physical step:** swap the second drone to brushless (removes a whole class of
  cross-platform config-drift risk), then validate uSD (on-board) logging on both drones,
  which has never been exercised in an actual 2-drone flight.
- **Not yet started:** the actual C.1 residual data collection, and everything downstream of
  it (training the residual model, integrating the seven comparison strategies, the final
  systematic comparison).

## 4. Figures, plots, videos

- **Controller Validation Card** (status page): https://claude.ai/code/artifact/c3deffc6-a90e-43ac-8a1c-f21f086106df
- **Two Tracks to the Thesis** (overall progress page): https://claude.ai/code/artifact/d2e524c4-c41d-4a54-99e7-267b7a43dd27
- **Two Ways to Build INDI** (our INDI vs. the reference authors' implementation): https://claude.ai/code/artifact/45a5f215-c23a-4a5b-8d66-45855c403fe0
- <Add: any flight video from the first 2-drone flight, and the downwash roll-std comparison
  plot, if worth showing.>

## 5. Questions / decisions needed

- The H0 partition (isolating position-INDI vs. attitude-INDI) came back inconclusive — both
  sub-loops are individually worse than running both together. Is it worth more lab time
  isolating this further, or should I accept the current oscillation, freeze the gains, and
  move on to data collection with that known limitation documented as a threat to validity?
- Is a standard (non-brushless) second drone acceptable at all for any part of the campaign,
  or does the protocol require both drones brushless from the very first real dataset?
- <Add any other open questions before the meeting.>

## 6. Plan until next meeting

1. Swap the second drone to brushless.
2. Validate uSD logging on both drones in a real 2-drone flight.
3. Fly a real notch-filter test to confirm the offline fix.
4. Reach a decision on the residual-oscillation question and freeze the gains.
5. Begin real C.1 data collection (A1 → A3 → A4, per the minimum-viable-dataset requirement).

## 7. Notes from the meeting (filled in during/after)

<Supervisor feedback, decisions made, anything that changes the plan above.>

## 8. Action items

- [ ] Swap `cf_second` to brushless
- [ ] Validate uSD logging on both cards
- [ ] Fly `notch_en=1` test to confirm the offline fix
- [ ] Freeze the gains
- [ ] Begin C.1 data collection
