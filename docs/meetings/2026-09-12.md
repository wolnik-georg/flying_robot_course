# Meeting — 2026-09-12

**Since last meeting:** first meeting using this format — summarizing the whole project to date
**Attendees:**
**Weeks to deadline:** 24

---

## 1. Summary

- Software preparation is finished: controllers, formation library, simulator, residual pipeline.
- First-ever 2-drone flight achieved this week.
- Both blocked on the same thing now: a controller-validation question (residual oscillation),
  gains not frozen yet.
- Once resolved: real data collection (C.1) starts.

## 2. What I did since last time

**Literature** — read multiple times, summary written per paper:

*Core:*
- Cobo-Briesewitz et al. — Learned INDI for Quadrotors (Neural-Augmented INDI)
- Gielis et al. — Modeling Aggregate Downwash Forces
- Hsieh et al. — Flatness-Preserving Residual Learning
- Shi, Hönig et al. — Neural-Swarm2
- Smith et al. — SO(2)-Equivariant Downwash Models

*Additional:*
- Shi et al. — Neural-Swarm (original)
- Zhang et al. — ProxFly (residual RL)
- Smeur et al. — Cascaded INDI
- Tal & Karaman — INDI + differential flatness
- Kiran et al. — Static/dynamic downwash interactions
- Chee, Hsieh, Pappas, Hsieh — KNODE-DW MPC
- Hsieh, Chee, Hsieh — L1 KNODE-DW MPC

**Formation library** — 16 scenarios, prepared + sim-validated, 2- and 3-drone
(`docs/10_Formation_Library.md`):
- Two-robot (A1–A8): vertical stack hover/tracking, static-top, offset stack, reverse-circle
  stack, extreme stack, dynamic merge, vertical swap.
- Three-robot (B1–B3): I-stack, V-stack, vertical swap.
- Coverage/control (C1–C5): side-by-side, leader-follower, triangle, docking, near-ground pass.

**NA-INDI reference repo:**
- Initial look at the authors' own code/firmware.
- Identified 3 variants: INDI, IL-NDI, NA-INDI.
- Confirmed our INDI is algebraically equivalent to theirs.
- Not integrated or run yet — possible next step, see §6.

**Controller & platform commissioning** (many weeks, see `docs/07` History):
- Geometric SE(3) + all 4 INDI modes, one Rust controller, same source on hardware and in sim.
- Three platforms from one switchable config: standard / thrust-upgraded / brushless CF2.1.
- Residual force measurement (`indi.a_res_*`) logged in every controller mode.
- Two real flight-code bugs found + fixed: residual sign was reinforcing disturbances (2.00×);
  `a_res` was dead under geometric.
- Residual-learning infra built end to end (onboard net, training pipeline, sim dry run) —
  sim-proven only.

**Controller validation, 09-09 → 09-12** (`docs/FLIGHT_CARD_VALIDATION.md`, `docs/lab_sessions/`):
- 09-09: six crashes, three root causes found + fixed.
- 09-11: re-flew clean (stage 1). Stage 2: oscillation reduced 88° → 7°, still no clean pass.
- 09-12: H0 test (attitude-INDI vs. position-INDI alone) — inconclusive, both worse alone.

**First-ever 2-drone flight, 09-12** (`docs/lab_sessions/2026-09-12.md`):
- Six infrastructure bugs found + fixed (gains, hand-off, hover, arming, logging, landing).
- No uSD logging / plots yet — but the obvious observation: lower drone visibly disturbed by
  downwash, upper drone steady.
- Follow-up on our own controller crashed both drones — root-caused + fixed, not re-flown.

**Architecture decision, 09-13:** `cf_second` permanently pinned to stock Lee, regardless of
platform. Only `cf231_active` is ever the controller under study. Removes the cross-platform
gain-sharing risk by construction — no brushless swap needed for `cf_second` any more.

## 3. Where I am now

- **Done:** software prep, formation library, simulation, residual measurement, residual
  infra (sim-only), first 2-drone flight.
- **Open, blocking:** residual oscillation under INDI (stage 2) still unresolved, gains not
  frozen. `kr`/`kw` + `pos_gains` retuning is the path forward.
- **Next step:** continue `kr`/`kw` + `pos_gains` retuning, then validate uSD logging on both
  drones.
- **Not started:** C.1 data collection and everything after it.

## 4. Figures, plots, videos

## 5. Questions / decisions needed

## 6. Plan / action items

**Neither our own geometric nor INDI has actually worked in a 2-drone scenario yet** — only
stock Lee has (`a_res=0`, no real data). 09-14 plan:

- [ ] Geometric, 2 drones, A1 hover — closes C.0 flight 5, first real C.1 data point
- [ ] Full INDI, 2 drones, A1 hover — ahead of C.0's strict order, A1 doesn't hit the open shake
- [ ] Validate uSD logging + sync, both drones
- [ ] If clean: move to A3
- [ ] Continue the `kr`/`kw` + `pos_gains` coordinated retuning line (separately, not blocking)
- [ ] Decide on the oscillation question, freeze gains

**Later, not scheduled:** IL-NDI / NA-INDI need real data to run at all — added after
collection starts, not before. Brushless swap for `cf_second` — its own session, not required
for correctness any more.
