# Lab Session — 9 September 2026

**Goal:** Get today's desk-work commits (onboard `e_R` logging, `experiments/analysis/`
pipeline) onto the lab PC and the physical drone, then resume C.0 at rung 0/1 (bench checks,
single-drone hover) per `docs/C0_FLIGHT_CARD.md`.

Machine: lab PC (separate from the desk-work sandbox). Repos: `flying_robot_course`,
`crazyswarm2`, `crazyflie-firmware`.

---

## 1. What happened, in order

1. Desk-work sandbox pushed `flying_robot_course` (commit `19036f2`, "new scripts for
   analytics" — includes the onboard `e_R` logging added 2026-09-08 and the
   `experiments/analysis/` pipeline built 2026-09-08/09) and `crazyswarm2` (commented-out
   `indi_e_r` radio topic block) to `main`. Both were previously unpushed working-tree state
   only.
2. Confirmed `crazyflie-firmware`'s two new sim-only bindings changes
   (`bindings/cffirmware.i`, `bindings/setup.py` — e_R getters for the SIL/host build) do
   **not** need to reach the lab PC; only its two pre-existing, always-local hardware mods
   (`usddeck.c` 40-var budget, `controller_indi.h` filter/gain re-derivation, see
   `firmware_app/host/LOCAL_MODIFICATIONS.md`) matter for a physical flash, and those should
   already be present in the lab PC's own `crazyflie-firmware` clone from earlier sessions.
3. Provided lab PC command sequence: `git pull` both repos, verify the two
   `crazyflie-firmware` local mods are intact (`git status --short` = exactly those 2 files,
   `MAX_USD_LOG_VARIABLES_PER_EVENT` reads 40), `cd firmware_app && make cload` to flash the
   new `e_R` logging onto the drone, then confirm `indi.e_r_x/y/z/norm` in the log TOC plus
   the standard bench checks (`kv_xy=5.0`, `clamp_en=11`) before any propeller turns.

---

## 2. Commits made this session

| Repo | Commit | What |
|---|---|---|
| `flying_robot_course` | `19036f2` | Onboard `e_R` logging + `experiments/analysis/` pipeline (pushed from desk-work sandbox, not made in-lab) |
| `crazyswarm2` | (same push) | Commented-out `indi_e_r` optional radio topic block |

---

## 3. Gains in effect right now

Unchanged from `docs/lab_sessions/2026-09-07.md` §3 (`kr_geo=0.010/kw_geo=0.00110`,
`clamp_en=11`, shared position gains) — nothing gain-related touched today yet. **Confirm on
the vehicle before flying**, not from this table.

---

## 4. To-do — next in this session (or the next one)

- [ ] `git pull` `flying_robot_course` and `crazyswarm2` on the lab PC.
- [ ] Verify `crazyflie-firmware` local mods intact, `make cload`, confirm `e_R` in log TOC.
- [ ] Bench checks per `docs/C0_FLIGHT_CARD.md` §0 (`kv_xy`, `kp_xy`, `clamp_en`, `kr_geo`/
      `kw_geo`, uSD card present, roster = `crazyflies.yaml` not `_sim`).
- [ ] Confirm the three unflown residual changes (sign fix, `a_res` gating fix, `rnn.en=0`)
      are actually in the flashed build — none is detectable in single-drone flight.
- [ ] Rung 1: single-drone hover — pass = `|a_res|` small but not exactly 0, no ~1-1.5 Hz
      oscillation.
- [ ] Rung 2: **the sign-check flight** — A1 @ Δz=0.75, **same INDI law, compensation on vs.
      off** (not geometric vs. INDI — that conflates "residual sign correct" with "INDI tracks
      better than geometric," a separate later comparison). Analyse with
      `analyze_formation.py <timestamp>` for the live check; `run_analysis.py`/`plot_flight.py`
      once the uSD logs are off the card, for the full RMSE/e_R/plot picture.

## 5. Open questions carried into next time

- Whether INDI's historical oscillation (see `project_indi_oscillation_investigation`) is
  still present now that `clamp_en=11` is confirmed the right/frozen value — not re-tested
  since the 2026-09-07 session's revert.
- No dedicated geometric-only tuning ladder exists yet for the 41g brushless airframe
  (currently flying the standard 27g platform's `kr_geo`/`kw_geo` values, flight-safe but
  not necessarily optimal) — still open per 2026-09-07 §5.
