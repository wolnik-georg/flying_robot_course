# Lab Session — 15 September 2026

**Goal at open:** first real uSD-logged A8 flight (cards never formatted/exercised before
tonight), then use it to validate the whole uSD tooling chain end-to-end.

---

## 1. What happened, in order

**uSD card prep + first real uSD flight.** Formatted both cards, ran the (then-existing)
`check_usd_deck.py`, flew A8 on both drones. First real uSD recordings ever collected on this
project.

**uSD tooling built out.** Root-caused why the recordings were far longer than the actual
flight (`usddeck.c` doesn't start a new file per logging session — the old `check_usd_deck.py`
toggled `usd.logging` to test the deck, which appended straight into the flight's own file) and
why the flight had no discoverable position inside the recording (`run_formation.py` never
broadcast `usec.reset` before logging, unlike `formation_flight.py`/`flight.py`). Fixed both:
rewrote `check_usd_deck.py` to use the read-only `usd.bcUSD`/`usd.canLog` params instead of
touching `usd.logging`, and added the missing `usec.reset` broadcast to `run_formation.py`.
Built `find_flight_window.py` (cross-correlates measured position against the scenario's own
recomputed commanded trajectory to locate the real flight inside a long recording) and
`copy_usd_log.py` (safe, sha256-verified, wall-clock-named card extraction). Also caught and
fixed a bad assumption mid-session: a uSD log and a radio CSV that arrived together via
`git pull` are NOT necessarily the same flight (see `MEMORY.md`,
`feedback_usd_radio_log_matching_2026-09-15.md`) — they were a night apart.

**Verification flight → repeated hard crashes.** With all of the above fixed, ran a fresh A8 to
verify the chain end-to-end. Instead: both drones went "full nuts" after the swap/crossing,
every single attempt, regardless of `passes` (tried 1, 2, and 3). Two drones, four separate
flights, same failure signature every time: clean ramp-up, then a violent tumble (roll to
~180° on `cf_second`, large `vel_z`) at almost exactly the same *elapsed flight time* (t≈11–13s)
no matter how long the scenario itself was supposed to run.

**Root cause found: a controller-identity race in `apply()`.** `formation_flight.py`,
`run_formation.py`, and `flight.py` all share the same phase-transition helper: it broadcasts
the shared `stabilizer.controller`/`indi_gains`/`pos_gains` to every drone in one loop, then
re-pushes each drone's own `per_robot` override (e.g. `cf_second`'s permanent stock-Lee pin,
`cf231_active`'s temporary geometric pin) in a second, separate loop. Those are two distinct
radio round-trips, not one atomic write — in the gap between them, a pinned drone genuinely ran
the SHARED controller (tonight: our OOT/INDI controller, `ctrl_mode=3`) with gains tuned for a
different airframe's mass/kt, for however long the round-trip took. That is a real, physical
bad-torque command, not a logging artifact: `cf_second`'s own `indi.a_res_z` spiked to ~‑8/‑9
m/s² at exactly the tumble onset, confirming a genuine unmodelled force, not corrupted
telemetry. Because `apply('scenario', ...)` always fires at the same point in the sequence
(right after trajectory upload, before `startTrajectory`), the glitch lands at the same elapsed
time regardless of scenario length — explaining why 1/2/3-pass flights all broke identically.
The battery-voltage sag that first looked like a possible cause was correctly identified as a
symptom, not the cause (the violent attitude correction is what draws the current that sags
the battery, not the other way round).

**Fixed in all three files.** `apply()`/`_apply_config()` now skip a param in the shared
broadcast loop entirely whenever that drone has its own `per_robot` override for it — the
override loop is the only place such a param is ever written, so there is no longer a window
where a pinned drone runs the wrong controller. Non-overridden drones/params are broadcast
exactly as before; behaviour for single-drone or no-override flights is unchanged.

**Not yet done tonight:** a fresh verification flight with the fix in place. The uSD chain
itself (`check_usd_deck.py`, `find_flight_window.py`, `copy_usd_log.py`) was never exercised
end-to-end because the crash investigation took over the rest of the session.

---

## 2. Commits

- `crazyswarm2`: rewrote `check_usd_deck.py` to non-invasive read-only checks; added
  `usec.reset` broadcast to `run_formation.py`; fixed the `apply()`/`_apply_config()`
  controller-identity race in `formation_flight.py`, `run_formation.py`, and `flight.py`.
- `flying_robot_course`: added `find_flight_window.py`, `copy_usd_log.py`; rewrote
  `README_usd_thesis_logging.md`; `.gitignore` cleanup (cflib `rw_cache`); this doc.

(Exact hashes: see `git log` in each repo for tonight's date — not restated here to avoid
drift if anything gets amended before push.)

---

## 3. To-do / next session

1. **Re-fly A8 with the `apply()` fix** — this is the actual end-to-end verification, both of
   the crash fix and of the uSD chain (`check_usd_deck.py` → fly → `copy_usd_log.py` →
   `find_flight_window.py`), which never got exercised tonight.
2. If clean: consider packaging the uSD workflow as a Claude Code Skill (raised tonight,
   deliberately deferred until after one clean real-world run with the fixed pipeline).
3. Physically inspect both drones (props, motor mounts, frame) before the next flight — the
   crashes tonight were hard tumbles, not soft oscillations.
4. `cf231_active`'s stale 2026-09-14 `ctrl_mode: 0` / geometric `pos_gains` override in
   `crazyflies.yaml` (lines ~31–50) is still unresolved and separate from tonight's bug — it's
   a real inconsistency (meta.json says `ctrl_mode=3` but `cf231_active` has been quietly
   flying geometric this whole time) that should be deliberately resolved either way.

---

## 4. Open questions

- Does the same controller-identity race exist anywhere else that broadcasts params outside
  `apply()`/`_apply_config()` (e.g. any one-off `setParam` calls elsewhere in these three
  files)? Not audited beyond the three `apply` functions themselves.
- `cf231_active`'s radio log from the very first crash flight (18-53-15) was entirely zero —
  no telemetry captured at all. Cause not investigated (a radio dropout coincident with the
  crash, or a pre-existing logging gap) — worth a look if it recurs.
