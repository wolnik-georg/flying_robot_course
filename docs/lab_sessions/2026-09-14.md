# Lab Session — 14 September 2026

**Goal at open:** the notch-filter approach is abandoned (2026-09-13's offline bode analysis —
its rejection band sits on the attitude loop's own natural frequency, `experiments/analysis/
notch_filter_bode.py`). Today: re-enable `cf_second`, continue the `kr`/`kw` + `pos_gains`
coordinated retuning line instead of the notch, then validate uSD logging on both drones.

---

## 1. What happened, in order

<fill in as the session goes>

## 2. Commits

## 3. Config at close

## 4. To-do / next session

## 5. Open questions
