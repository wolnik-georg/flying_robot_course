# Lab Session — 14 September 2026

**Goal at open:** the notch-filter approach is abandoned (2026-09-13's offline bode analysis —
its rejection band sits on the attitude loop's own natural frequency, `experiments/analysis/
notch_filter_bode.py`). Neither of our own controllers has actually worked in a 2-drone
scenario yet — the only clean 2-drone flight so far was stock Lee (`a_res=0`, no real data);
the one attempt at our own geometric on both drones crashed (root-caused and fixed 2026-09-12,
`cf_second` now permanently pinned to stock Lee). Today's plan, in order:

1. **Geometric, 2 drones, A1 hover** (`cf231_active`=our geometric, `cf_second`=stock Lee) —
   no known blocker, do this first. This both closes out C.0 flight 5 (`docs/11`) and *is* the
   first real C.1 data point (A1, geometric).
2. **Full INDI, 2 drones, A1 hover.** A1 is pure hover, which INDI has been clean and
   repeatable on the whole project — the open trajectory-tracking shake (circle/figure8) isn't
   exercised here. Note: this technically skips ahead of C.0's strict required order (flight 4,
   figure-8 under corrected INDI, hasn't reached a clean pass) — a deliberate, informed
   decision to try anyway since A1 doesn't touch the open issue, not an oversight.
3. **uSD logging + sync validation on both drones** — exercise this during (1) and (2) rather
   than as a separate hover-only check.
4. **If 1–3 go clean:** move to A3 under whichever controller(s) worked — real C.1 progress.

Corrected-INDI gain-freezing and the `kr`/`kw` + `pos_gains` retuning line (the actual fix for
the trajectory-tracking shake) are separate, ongoing work — not blocking today's plan, since
today doesn't require trajectory tracking under INDI.

---

## 1. What happened, in order

**Supervisor meeting, same day.** Feedback on the weekly meeting doc (too AI-toned, simplify).
Confirmed next steps: uSD logging validation (already today's plan), a firmware uSD timing bug
(below), the INDI oscillation needs an actual fix (not just documented), and Neural-Swarm2
should be added as a core comparison strategy. Full plan: see the 2026-09-14 meeting notes /
`MEMORY.md`.

**uSD timing bug, root-caused and fixed (desk time).** Each drone's `usecTimestamp()` free-runs
from its own power-on with no shared reference, so uSD log timestamps carried a constant
per-drone offset. Stock firmware already exposes the fix as a broadcastable param
(`usec_time.c`'s `usec.reset`, documented upstream for exactly this: UAV time sync) — we were
just never calling it. Added `allcfs.setParam("usec.reset", 1)` as its own broadcast right
before the uSD-logging-start broadcast in `formation_flight.py`, mirrored in
`flight.py`/`simple_flight.py` (`crazyswarm2` `932d901`). Not yet flight-tested — worth
confirming today's uSD sync check actually shows a tighter offset than before.

**NA-INDI faithful port, implemented and numerically verified (desk time, before lab).**
Supervisor judged the residual oscillation unacceptable as a known limitation; decided to port
Cobo & Briesewitz's `controller_lee.c` bit-for-bit rather than keep iterating on our own INDI.
New, fully independent controller slot `stabilizer.controller=7` (`ControllerTypeOot2`),
alongside the existing `=6` — separate enum, dispatch row, Rust module
(`firmware_app/src/naindi.rs`), no shared state. Required one further local
`crazyflie-firmware` change (`sensorData_t.gyroNoLpf`, ported verbatim) so the port reads the
exact same unfiltered gyro the reference does. **Numerically verified against the reference's
own compiled C**: 5 hand-picked non-trivial states match thrust and torque to ~1e-9. Exactly 4
documented, physically/architecturally necessary deviations; everything else identical. Builds
clean on all three platforms. Switchable via a plain `controller: 7` in `crazyflies.yaml`, same
as any other controller. **Never flown.** Full record: `flying_drone_stack` `4fbe013`+`c089511`
+`95182f1`, `firmware_app/host/naindi_reference_build_notes.md`.

**Neural-Swarm2 exact-architecture port, implemented (desk time, before lab) — does not fit in
RAM, and is not to be fixed by touching the network.** `residual_nn.rs` replaced with a direct
port of Neural-Swarm2's own `phi_Net`/`rho_Net` from their vendored reference code
(`crazyswarm2/.../neuralswarm.py`), not the smaller custom net that was there before. 19297
weights (~77 KB), up from 987 (~4 KB) — a real `make DRONE=bl` link overflows RAM by ~39 KB,
confirmed not estimated. **Operator's explicit, standing decision, after being offered pruning
options: leave the network exactly as ported, "out of the box," overflow included — never fix
this by modifying the network.** Does not currently build into a flashable firmware.
`test_residual_nn.py`, `test_pipeline.py` and `train.py` all still target the old shape and need
rewriting before their output means anything. Commit `a45ea03`.

## Next steps for today's lab session

The original plan above (geometric 2-drone A1 hover → full INDI 2-drone A1 hover → uSD sync
validation → A3 if clean) is unchanged and remains the priority — nothing from the desk-time
work above should displace it.

**Controller=7 (the NA-INDI port) is numerically verified but has never touched a real motor.**
It is not part of today's plan by default. If, and only if, the plan above finishes cleanly with
time remaining, a cautious **single-drone, hover-only** test of `stabilizer.controller=7` would
be the right next step — on a tether if one is available, well clear of anyone, with the same
watchfulness as any first flight of a new control law. Do **not** attempt it 2-drone or on any
trajectory yet; a numerically-verified port is not the same as a flight-verified one, and it
has not cleared any hardware gate.

**In-lab, evening: three real bugs found and fixed, live, via the desk↔lab git loop.**

1. **`flight.py` had never received the per-robot-override repush fix** that `formation_flight.py`
   got 2026-09-12 (`fdfc640`) and `run_formation.py` got earlier today. Its own uniform
   `stabilizer.controller`/`indi_gains`/`pos_gains` broadcast silently wiped `cf_second`'s
   permanent `stabilizer.controller=5` pin on every phase change (takeoff→trajectory→landing),
   in three separate places in the file. All three fixed (`crazyswarm2` `4c279e5`). A related
   logging bug found in the same pass: the `*_stabilizer_controller`/`*_ctrl_mode` CSV metadata
   always echoed the shared `all:` yaml default, never the actual post-override value — fixed to
   log the effective value instead (`6cf72aa`).
2. **`cf_second` (stock Lee, standard CF2.1) shows a consistent ~0.40 m height SHORTFALL**,
   confirmed via a 4-point isolated sweep (commanded 0.6/0.9/1.25/1.5 m → achieved
   0.20/0.50/0.85/1.09 m — a *constant* offset, not one that grows with height, ruling out
   PID/thrust saturation). Also reproduced in the first live A8 attempt (1.25 m target → 0.86 m
   achieved, `|ez|`=244 mm in `run_formation.py`'s own realised-geometry check, plus a genuine
   mid-flight crash-and-recover at t≈16 s). Mocap ceiling ruled out (1.5–1.7 m clearance).
   Battery voltage ruled out (comparable `vbat` on both drones). Operator recognised this from
   past standard-drone sessions — most likely cause is a ~0.4 m mocap rigid-body Z-calibration
   error specific to this vehicle (`cf231_active` shows no such offset). **Temporary
   code-side compensation added** (`+0.40m` keyed by robot name) in all three flight scripts —
   `flight.py`, `run_formation.py`, `formation_flight.py` (`crazyswarm2` `53bb61c`) — to be
   removed once the mocap rigid body is recalibrated properly.
3. **First A8 re-fly after both fixes: clean.** `cf231_active` (bottom) held ~0.96–0.99 m,
   `cf_second` (top) held ~1.22–1.29 m against a 1.25 m target — correct relative ordering for
   the first time tonight, no crash, roll/pitch tame on both throughout.

**uSD logging not yet exercised** — realized mid-session that neither drone's uSD card has ever
been formatted + given `config.txt` (`flying_drone_stack/tools/usd_thesis_config.txt` copied to
the card root under that exact name). Deferred to tomorrow; exact prep steps written to
`debug/lab_logs/debug.log` (`lsblk` → `mkfs.vfat -F 32` → copy `config.txt` → `check_usd_deck.py`
per card, one card per drone).

## 2. Commits

`crazyswarm2`: `4c279e5` (flight.py per-robot repush fix), `6cf72aa` (effective-value logging
fix), `69eed32`+`389ae07` (cf231_active enable/disable toggles around the isolation test),
`5030e27` (disable for height sweep), `53bb61c` (0.40m height compensation, all 3 scripts).
`flying_robot_course`: `6ae7035` (height-sweep test commands), `be74245` (uSD card prep steps).

## 3. Config at close

`crazyflies.yaml`: `cf231_active.enabled: true`, `cf_second.enabled: true`, both back in normal
2-drone A8 configuration. `cf231_active`'s temporary geometric override (`ctrl_mode: 0`,
`pos_gains` 40/8/30/10) still in place — not yet removed, full-INDI step of the original plan
never reached tonight. `cf_second`'s +0.40m height compensation active in code (not yaml) —
applies automatically to any script that computes a height target for it.

## 4. To-do / next session

**Priority for tomorrow: get uSD logging working across both drones and confirm the sync check.**
1. Format both uSD cards, copy `config.txt`, confirm detection with `check_usd_deck.py` (steps
   in `debug/lab_logs/debug.log`).
2. Re-fly A8 (already clean tonight) with `usd.logging` now actually recording — `run_formation.py`
   already toggles it on/off automatically, no extra flag needed.
3. Pull both cards' log files, run `tools/merge_usd_logs.py`/`decode_usd_log.py` (never run on
   real 2-drone data before) and check the `usec.reset` time-sync fix (2026-09-14 desk work,
   never flight-tested) actually tightens the cross-drone offset.
4. If clean: continue with the original plan — full INDI 2-drone A1/A8, then A3 if that's clean
   too (real C.1 data collection).
5. Separately, when there's a free moment: recalibrate `cf_second`'s mocap rigid body properly
   and remove the temporary `_Z_OFFSET_COMPENSATION` from the three flight scripts.

## 5. Open questions

- Is the ~0.40 m `cf_second` height offset really the mocap rigid body, or could it be something
  else entirely coincidentally landing at the same magnitude at every commanded height? The
  fixed-offset behavior across 4 very different targets makes mocap calibration the strongest
  hypothesis, but it hasn't been directly confirmed by inspecting the rigid-body definition
  itself.
- `cf231_active` showed a mild roll excursion (~-26°, recovered) around t≈23s in the clean A8
  re-fly — not investigated; possibly the crossing/wash event, possibly unrelated.
- The `19-32-16` A8 attempt (before the good one) shows `cf231_active` crashing hard (`roll`
  ±179°) very early, before the trajectory phase even started logging — cause not established,
  worth asking whether anything unusual was visible in the lab at that moment.
